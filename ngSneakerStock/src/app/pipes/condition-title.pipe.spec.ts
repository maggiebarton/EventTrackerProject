import { ConditionTitlePipe } from './condition-title.pipe';

describe('ConditionTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
